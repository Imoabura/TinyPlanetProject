using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrbitOrientation : MonoBehaviour
{
    [SerializeField]
    Transform orbitTarget;  // the body you're orbiting around

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
